// Thin shim: existing routes import { Prepare } from "./pages". The actual
// editor lives in src/components/editor/ as a set of focused components.
export { PrepareEditor as Prepare } from "../components/editor";
